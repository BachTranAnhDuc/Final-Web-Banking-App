import styled from 'styled-components';

const HistoryStyled = styled.main`
  .section-history {
    background-color: var(--color-grey-light-9);

    min-height: 85vh;

    padding: 8rem 12rem;
  }
`;

export default HistoryStyled;
