import styled from 'styled-components';

const ManageAccountStyled = styled.main`
  .section-manageAccount {
    background-color: var(--color-grey-light-9);

    min-height: 85vh;

    padding: 8rem 12rem;
  }
`;

export default ManageAccountStyled;
